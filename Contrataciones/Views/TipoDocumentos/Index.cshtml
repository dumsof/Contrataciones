@model IEnumerable<Contrataciones.Models.TipoDocumentos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionDocumento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionDocumento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoDocumentoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoDocumentoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoDocumentoID })
        </td>
    </tr>
}

</table>
